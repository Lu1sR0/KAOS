/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "api.microlink.io", // Microlink Image Preview
      'avatar.vercel.sh',
    ],
  },
};

export default nextConfig;
